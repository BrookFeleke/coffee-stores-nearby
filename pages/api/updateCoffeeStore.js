import { table, getMinifiedRecord } from '../../lib/airTable';

const updateCoffeeStore = async (req,res)=> {

const {method } = req
    const {id,name,address,neighborhood,upvote} = req.body
    
    if(method === 'POST'){
        
    }
}
